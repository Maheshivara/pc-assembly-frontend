import { AssemblyWizard } from "@/components/AssemblyWizard";

export default async function AssemblyPage() {
  return (
    <div>
      <div>
        <AssemblyWizard />
      </div>
    </div>
  );
}
